export default {
    plugins: {
        // Tailwind CSS sebagai plugin PostCSS
        tailwindcss: {},
        // Autoprefixer (untuk menambahkan prefix vendor pada CSS)
        autoprefixer: {},
    },
};